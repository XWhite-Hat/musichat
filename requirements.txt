PySide6>=6.11.2
customtkinter>=5.2.0
sounddevice>=0.5.6
numpy>=2.5.2
twitchio>=3.3.2,<4.0.0
requests>=2.34.2
fastapi>=0.141.1
uvicorn[standard]>=0.52.4
PyJWT>=2.13.0
cryptography>=50.0.0   # <50 has GHSA-g6cj-pr64-35w5 (Bleichenbacher oracle, HIGH)
keyring>=25.7.0
python-multipart>=0.0.32
websockets>=16.1.1
httpx>=0.28.1
yt-dlp>=2026.8.19,<2027.1.0
Pillow>=12.3.0         # <12.3 has GHSA-45hq-cxwh-f6vc (decompression-bomb bypass, HIGH)

# Transitive (twitchio, yt-dlp) - floor pinned here for security only:
# <3.14.3 has GHSA-cq5v-8q36-5273 (OOB heap read in the C parser, HIGH)
aiohttp>=3.14.3

# -- av: pinned exactly, do not bump automatically ---------------------------
# The shipped binary does NOT use the PyPI av wheel - that one bundles GPL
# x264/x265.  It uses vendor/av-17.1.0-cp314-cp314-win_amd64.whl, an LGPL-only
# build against FFmpeg 8.1, force-reinstalled over the PyPI one at build time.
#
# This pin must therefore match the vendored wheel exactly.  If it drifts above
# it, the lockfile and the generated SBOM record a version that is not what
# ships - which defeats the point of vendoring an LGPL build at all.
#
# To move it: rebuild the wheel with scripts/build-lgpl-av.ps1, update
# vendor/SHA256SUMS and the filename in ci.yml / release.yml / build.ps1,
# and only then bump this line.  Dependabot is configured to ignore av.
av==17.1.0

# keyboard>=0.13.5  # optional - global media keys; install manually if needed
